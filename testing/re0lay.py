#-*-coding:cp1254-*-
#github.com/s0l0n3t/modules
import sys
import os
import random
import time
sys.path


class sepp:
    def __init__(self):
        self.system = os.name
        self.abc = ["a","b","c","d","e","f","g","h","j","k","l","m","n","o","p","r","s","t","v","y","z"]
        self.number = ["1","2","3","4","5","6","7","8","9","0"]
	

slain = sepp()


def files():
	for i in seath:
	    print a1 + i
	dosya = open("hash.txt","a")
	dosya.write("\n%s\n"%(a1 + i))
	dosya.close

def main():
 try:
    if slain.system  in "nt":
        print "for exit :Ctrl + C\n"
        print 
    seath = []
    a1 =(random.choice(slain.number)+random.choice(slain.abc))
    global seath,a1
    return seath.append("".join(random.choice(slain.abc)+random.choice(slain.abc)+random.choice(slain.abc)+random.choice(slain.abc)+random.choice(slain.abc)+random.choice(slain.number)+random.choice(slain.number)+random.choice(slain.abc)+random.choice(slain.abc)+random.choice(slain.abc)+random.choice(slain.number)+random.choice(slain.number)+random.choice(slain.abc)+random.choice(slain.number)))
 except KeyboardInterrupt:
        timi =int(time.clock())
        print "Second : %s"%(timi - tim)
        sys.exit()

tim = int(time.clock())
global tim

while True:
    main()
    files()
    continue
    
    
    
    
	
